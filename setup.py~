from setuptools import find_packages
from setuptools import setup

setup(name='contractvm',
	version='0.1',
	description='ContractVM daemon',
	author='Davide Gessa',
	setup_requires='setuptools',
	author_email='gessadavide@gmail.com',
	packages=['contractvm', 'contractvm.backend', 'contractvm.hw', 'contractvm.tst', 'contractvm.fifo', 'contractvm.eth', 'contractvm.bs', 'contractvm.chain'],
	entry_points={
		'console_scripts': [
			'contractvmd=contractvm.contractvmd:main',
		],
	},
)

setup(name='libcontractvm',
	version='0.1',
	description='ContractVM library',
	author='Davide Gessa',
	setup_requires='setuptools',
	author_email='gessadavide@gmail.com',
	packages=['libcontractvm']
)
